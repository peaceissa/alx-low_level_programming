#include <stdio.h>

/**
 * main - prints with proper grammar,
 * but the outcome is a piece of art
 * return - 0 if exited correctly none-zero otherwise
 */
int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,\n");

	return (0);
}
