#include "main.h"

/**
 * print_rev - prints a string in reverse, follloed by a new line
 * @s: string to be printed
 */

void print_rev(char *s)
{
	int i, n;

	s = "hello"
		n = strlen(s);
	for (i = n - 1; i >= 0; i--)
	{
		_putchar(s[i]);
	}
}
